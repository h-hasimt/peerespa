<% if current_admin.role == "support" then %>
<%= render 'support' %>
<h2> 私はサポーターです </h2>

<% elsif current_admin.role == "manage" then %>
<%= render 'manage' %>

<% else %>
  <h2> 許可されていないユーザです。ログアウトしてください。 </h2>

<% end %>

<%= link_to "サポータログアウト", destroy_admin_session_path, method: 'get' %>
